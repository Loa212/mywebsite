---
import { Image } from "astro:assets";

export interface Props {
  image: ImageMetadata;
  alt: string;
}

const { image, alt } = Astro.props;
---

<Image
  class:list="object-contain w-full z-0"
  src={image}
  height={image.height}
  width={image.width}
  alt={alt}
/>
